# ADR: 커밋된 산출물의 「검증 가능한 거짓」 기계 대조 — 인용 식별자·박제 술어 양축 기각, 감시값 SSoT 수렴만 채택 (#1031 / #1064)

- 일자: 2026-08-14
- **상태**: **Accepted** (cross-validate agy 2026-08-14 — `code 1074`, anchor `ADR-new-or-amendment`, outcome `applied`. §교차검증 반영 사항 4축 통합 완료. `Provisional` 에서 전이 — CLAUDE.md §ADR Status 워크플로, [#370](https://github.com/coseo12/astro-simulator/issues/370) 옵션 C). ⚠️ 본 라인은 [`20260812-1005`](20260812-1005-adr-index-status-guard.md) §재검토 조건의 **어순 제약** 대상이다 — 현재 상태 토큰이 최선두여야 한다
- 관련: 이슈 [#1031](https://github.com/coseo12/astro-simulator/issues/1031) (인용 식별자 축) / [#1064](https://github.com/coseo12/astro-simulator/issues/1064) (박제 술어 축) / 후속 [#1075](https://github.com/coseo12/astro-simulator/issues/1075) (잔여 2건) / 근거 PR [#1023](https://github.com/coseo12/astro-simulator/pull/1023) (블로킹 4건) · [#1061](https://github.com/coseo12/astro-simulator/pull/1061) (수치 오류 4라운드)
- **병행 PR** (파일 공유 — 판정·인계는 §동시 진행 PR 과의 관계): [#1072](https://github.com/coseo12/astro-simulator/pull/1072) (§결정 5 를 이미 이행 + `982` 앵커 충돌) / [#1070](https://github.com/coseo12/astro-simulator/pull/1070) (`983` (ii) 확장 동일 삽입점) / [#1069](https://github.com/coseo12/astro-simulator/pull/1069) (본 PR 무접촉)
- 선행 ADR: [`20260808-983`](20260808-983-measurement-recording-convention.md) §기각 2 · §Amendment 2 (동일 클래스 2연속 기각 — 본 ADR 이 세 번째) / [`20260814-982`](20260814-982-changelog-tilde-guard.md) §재검토 조건 1 · §후속 감시 (감시값의 거처) / [`20260814-958`](20260814-958-prettier-live-docs-scope.md) §고유 발견 (#1064 발의 출처) / [`20260811-1010`](20260811-1010-measurement-c-verdict-tiers.md) §결정 3 (3계급 선례) · §결정 7 (SSoT 수렴 선례) / [`20260812-970`](20260812-970-pr-base-rule-guard.md) §8-1 5-b (#1031 경미 사항 대상)
- **측정 rev**: `38b6c8a` (`origin/develop` tip, 2026-08-14). 본 문서의 모든 수치는 이 단일 rev 에서 일괄 도출했다 ([`20260808-983`](20260808-983-measurement-recording-convention.md) §(i) 부분 재측정 금지). 네트워크 조회분(GitHub API)은 같은 일자 GET 이며 rev 를 갖지 않는다 — 아래 §술어 정본 규약 3항 참조

---

## 통합·분리 판정 — 한 ADR 로 합친다 (#1064 완료 기준 3항)

#1064 완료 기준이 명시적으로 요구한 판정이다. **통합**한다.

**축은 실제로 다르다** — #1031 은 *"인용 **식별자**(CI run id / 커밋 SHA)를 API 로 대조"*, #1064 는 *"박제 **술어**를 재실행해 박제값과 대조"* 다. 그럼에도 합치는 근거 4항:

1. **판정이 갈리지 않았다.** 두 축이 각각 독립 측정으로 **같은 결론(기각)** 에 도달했고, 잔여 처방도 **같다**(SSoT 수렴 — 아래 §결정 5). 판정이 갈렸다면(한쪽 채택 / 한쪽 기각) 분리가 **의무**였다. 이 반례 조건을 명시해 둔다
2. **측정 인프라가 공유된다.** 두 축 모두 같은 코퍼스(`docs` `.claude` `CLAUDE.md` `CHANGELOG.md`)를 같은 rev 에서 스캔했고, 기각 근거의 뼈대인 **시간 종속 5분류**(`T0`~`T4`)는 두 축에 공통 적용된다
3. **분리하면 본 ADR 이 겨누는 결함을 스스로 생성한다.** 두 문서가 서로의 수치를 인용해야 하고, 그 순간 [`20260808-983`](20260808-983-measurement-recording-convention.md) 오기록 4(부분 재측정)·6(술어 비교환)의 **다문서 판본**이 만들어진다. 규약을 만들면서 그 규약이 잡아낼 대상을 남기는 자기모순은 #983 초판이 이미 밟은 경로다
4. **[`README.md`](README.md) §규약 11 (_"1 ADR = 1 결정"_) 과 정합한다.** 본 ADR 의 결정은 **하나** — _"커밋된 산출물 안의 「검증 가능한 거짓」에 대해 기계 대조 가드를 도입하지 않는다"_ 다. #1031·#1064 는 그 결정의 **두 인스턴스**(식별자 축 / 술어 축)이지 두 결정이 아니다. #1031 스코핑 질문 5번이 #1064 의 일반형이라는 계보가 이 동일성의 방증이다

### Forensic 변형 판정 — 일반 ADR 로 쓴다

CLAUDE.md §Forensic ADR 변형 5조건 중 **3항 충족**(가설 N≥2 / runtime 측정 필수 / 5±2 옵션 비교)이라 형식상 문턱(3개 이상)에 닿는다. 그럼에도 **일반 ADR** 을 택한다.

- **조건 3(_"DoD PASS 인데 사용자/제품 회귀"_)이 결정적으로 부재**하다. forensic 변형의 존재 이유는 *"사용자 인지 단위 ↔ 박제 단위 mismatch"* 의 추적인데, 본건은 **회귀 원인 규명이 아니라 정책 판정**이라 그 축이 없다
- **동종 선례가 전부 일반 ADR** 이다 — [`20260813-1020`](20260813-1020-adr-index-membership-marker-rejected.md) (마커 기각) · [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 (병치 검출 기각). 같은 클래스에 다른 형식을 쓰면 세 기각의 대조표가 형식 차이로 읽기 어려워진다
- CLAUDE.md 가 규정한 안전판을 그대로 채택한다 — _"판정 애매하면 **일반 ADR 로 시작 후 Amendment 1회 필요해지면 forensic 으로 승격**"_. 승격 시 양방향 cross-link 를 건다

---

## 배경

PR [#1023](https://github.com/coseo12/astro-simulator/pull/1023) 의 블로킹 4건과 PR [#1061](https://github.com/coseo12/astro-simulator/pull/1061) 의 수치 오류 4라운드는 **한 클래스**다 — *"커밋된 산출물 안의, 검증 가능한 거짓"*. 코드 동작은 매번 정상이었고 틀린 것은 **근거·수치 문장**이었다.

### PR #1061 이 이 공백의 비용을 4라운드로 실측했다

| 라운드 | 형태 | 검출자 |
| --- | --- | --- |
| 1 | delta(`39`)를 감시값(계수) 자리에 | reviewer |
| 2 | 디렉토리 전체 파일수(`42`)를 PNG 계수 자리에 | reviewer |
| 3 | **술어를 적었는데 실행하면 `0`** — `git ls-files 'docs/{a,b}/'` 는 작은따옴표가 brace expansion 을 죽여 git 이 리터럴 pathspec 으로 읽는다 | 메인 독립 재현 |
| 3 | 자기참조 계수 노화(`hit 2` 가 자기 편집으로 `4`) / 술어 없는 절대수 | 메인 독립 재현 |

**결정적 비대칭** — 손으로 센 계수는 **4/4 틀렸고**, 기계가 뱉은 계수(`--population` / `prettier --file-info` / 가드 출력)는 **0/4 틀렸다**.

**라운드 3 이 설계상 가장 중요하다.** 라운드 2 의 처방이 *"술어를 명시하라"* 였는데 **그 명시된 술어가 실행되지 않았다.** 즉 [`20260808-983`](20260808-983-measurement-recording-convention.md) (iv) 의 표기 규약화는 **필요조건이지 충분조건이 아니다** — *"적은 술어가 그 값을 실제로 낸다"* 가 남는다. 이 잔여가 #1064 의 발의 근거다.

### 두 이슈가 지목한 공백은 같은 문장으로 표현된다

[`20260808-983`](20260808-983-measurement-recording-convention.md) §(ii)·(iv) 는 **규범으로 존재하지만 발화 지점이 0** 이라 매번 주의력에 의존한다. 이것은 PR #1061 이 `.prettierignore` 에서 고친 결함(*"선언은 있는데 동작이 0"*)과 **같은 형태**이며, 본 ADR 은 이 사실 자체를 결론과 무관하게 박제한다 (아래 §결정 6).

---

## 술어 정본 규약 (본 ADR 의 자기 적용)

본 ADR 은 *"박제 술어가 그 값을 내는가"* 를 다룬다. **자기 자신이 그 규약을 위반하면 그 자체로 결함**이므로 아래 3항을 스스로 지킨다.

1. **모든 절대수에 재현 가능한 술어를 동반한다.** 술어는 **rev 를 인자로 받는 형태**로 적는다 — 산문에 rev 를 적고 명령은 워킹트리 형태로 두면 결속이 자연어 대용(anaphora)이 되어 기계 재현이 성립하지 않는다 (이 실패가 아래 §측정 3 의 핵심 발견이다)
2. **박제 전에 술어를 추출해 그대로 실행하고 값이 재현되는지 확인했다.** 본 문서의 모든 rev 고정 술어는 작성 중 1회 실행됐다
3. **네트워크 조회분은 rev 를 갖지 않는다.** GitHub API GET 결과는 조회 일자(2026-08-14)로만 고정되며, 위 1항의 재현 보장 **밖**이다. 이 경계를 명시하지 않으면 *"재현 가능"* 이 과대 주장이 된다

---

## 실측

### 측정 1 — 축 A 모집단 (인용 식별자, #1031)

```bash
# (1-a) 고유 인용 hex 토큰 — 8자리 날짜 오탐 배제를 위해 a-f 문자 1개 이상 강제
git grep -ohE '`[0-9a-f]{7,10}`' 38b6c8a -- docs .claude CLAUDE.md CHANGELOG.md \
  | tr -d '`' | grep -E '[a-f]' | sort -u | wc -l          # 158
# (1-b) 고유 CI run id
git grep -ohE 'actions/runs/[0-9]{6,}' 38b6c8a -- docs .claude CLAUDE.md CHANGELOG.md \
  | sed 's|.*/||' | sort -u | wc -l                        # 19
```

2단 해석(로컬 `git cat-file -e '<sha>^{commit}'` → 실패 시 `gh api repos/coseo12/astro-simulator/commits/<sha>`) 결과:

| 축 | 모집단 | 해석 | 미해석 | **미교정 잔존 오류** |
| --- | ---: | ---: | ---: | ---: |
| (1-a) hex 토큰 | 158 | 141 | **17** | **0** |
| (1-b) CI run id | 19 | 19 | **0** | **0** |

미해석 17건 **전건 전수 분류** (기계 판정 불가 영역이라 표로 감사 가능하게 남긴다):

| 사유 | 건 | 토큰 |
| --- | ---: | --- |
| **하네스 manifest `sha256` 접두** — [`20260515`](20260515-harness-managed-divergent-pattern.md) §Z 패턴 C1/C2 표 (_"전수 대조로 … 2건 추가 발견"_ 문단 직후). **git 객체가 아니다** | 12 | `19108a4b` `2d056f6d` `30aedb41` `368c7de5` `587c5349` `621bbcb7` `997c8c07` `b1e12770` `bffdaa7a` `df781fc4` `eb8eac1e` `f7d50bbf` |
| 로컬 전용 preview 브랜치 (push 된 적 없음) | 3 | `73225a1` `83b8aae` `a69a5a5` |
| upstream `harness-setting` PR 맥락 SHA | 1 | `264c9d4` |
| squash 머지 + 브랜치 삭제로 소멸한 PR head | 1 | `c4a36c4` |

**17건 전부 정당하게 해석 불가하며, 고쳐야 할 오류는 0 이다.**

> ⚠️ **라벨 정정 (PR [#1074](https://github.com/coseo12/astro-simulator/pull/1074) reviewer 라운드 2).** 초판은 상위 12건을 _"upstream `harness-setting` SHA"_ 로 적었으나 실물은 그 표의 **manifest `sha256` 열**이며(6건은 `(= 로컬)` 값 / 6건은 `upstream v4.5.0` 열), **git 커밋 SHA 가 아니라 파일 내용 해시**다. 라벨과 실제 대상이 어긋난 것이라 [`20260808-983`](20260808-983-measurement-recording-convention.md) 오기록 **5** 형태이며 본 표에서 정정한다.
>
> **이 정정은 기각을 강화한다.** 추출기가 `[0-9a-f]{7,10}` 로 잡는 것 중 **12건은 애초에 커밋 SHA 가 아니다** — 즉 축 A 의 잡음은 *"해석 안 되는 SHA"* 가 아니라 ***"SHA 가 아닌 것을 SHA 로 읽는 것"*** 이고, 이는 정규식 정밀도로 좁혀지지 않는다(같은 문자류·같은 길이). §재검토 조건 5(_"추출기 정밀도 개선은 트리거가 아니다"_)의 근거가 하나 늘었다.

> ⚠️ **#1031 (b) 의 원 사례는 존재 검사로 잡히지 않는다.** 그 사례는 `201ca1c` 를 *"일회용 분리 커밋"* 이라 적었으나 실제로는 당시 PR head 였던 **귀속 오류**다. 그런데 `201ca1c` 는 **로컬에서 정상 해석된다** (`feat(ci): [#970] PR base 선택 규칙 CI 가드 …`). 즉 이 축의 발의 사례 자체가 존재 검사의 사각이고, 틀린 것은 **자연어 서술**이다.

### 측정 2 — 축 B 모집단 (박제 술어, #1064)

```bash
# 셸 펜스 개시행 (들여쓰기 허용)
git grep -chE '^[[:space:]]*```(bash|sh|shell|console)[[:space:]]*$' 38b6c8a -- docs \
  | awk -F: '{s+=$NF} END{print s+0}'      # 130
git grep -chE '^[[:space:]]*```(bash|sh|shell|console)[[:space:]]*$' 38b6c8a -- .claude \
  | awk -F: '{s+=$NF} END{print s+0}'      #  64
```

셸 펜스 **194** (`docs` 130 / `.claude` 64).

> ⚠️ **축 A 와 축 B 는 코퍼스 경계가 다르다** (reviewer 라운드 2 — 실해 `0` 이나 명시한다). 축 A(§측정 1)는 `docs .claude CLAUDE.md CHANGELOG.md` **4경로**, 축 B 는 **`docs` + `.claude` 2경로**다. 같은 rev 무제한 계수는 **`202`** 이고 차 `8` 의 내역은 `CHANGELOG.md` **3** + **`README.md` 계열 5**(루트 `README.md` **2** / `packages/core/README.md` **1** / `packages/physics-wasm/README.md` **2**)이며 `CLAUDE.md` · `.github` · `scripts` · `apps` 는 **전건 `0`** 이다. ⚠️ 초판은 이 `5` 를 _"그 밖(`.github`·`scripts` 등)"_ 으로 귀속했는데 **값은 맞고 귀속이 틀렸다** — 실측하면 그 두 경로는 `0` 이다 ([`20260808-983`](20260808-983-measurement-recording-convention.md) 오기록 5 형태. PR [#1074](https://github.com/coseo12/astro-simulator/pull/1074) reviewer 라운드 3). 축 B 가 2경로인 이유는 *"박제 술어 + 기대값"* 쌍의 거처가 **규범 문서**이기 때문이고, 축 A 가 `CHANGELOG.md` 를 포함하는 이유는 **인용 식별자가 릴리스 기록에 다수 등장**하기 때문이다. 두 모집단은 **포함 관계가 아니다** ([`20260808-983`](20260808-983-measurement-recording-convention.md) (iv) 확장 — 술어 비교환).

이 중 *"박제 술어 + 기대값"* 쌍을 추리기 위해 1회성 분류기를 적용했다 — 기대값 동반 **81**, 그중 모든 명령 줄이 읽기 전용 allowlist(`git ls-files` / `git grep` / `git rev-list` / `git log` / `node scripts/verify-*` / `pnpm verify:*` / `pnpm exec prettier --file-info` / `gh api`)에 드는 것 **8**.

> ⚠️ **`81` 과 `8` 은 1회성 분류기 산출이라 위 §술어 정본 규약 1항을 충족하지 못한다** — 분류기는 [`no-op-adr-pattern.md`](../lessons/no-op-adr-pattern.md) 의 debug 스크립트 규약(`scripts/_debug-<topic>-tmp.mjs`, 즉시 `rm`)을 따라 삭제됐다. 그래서 **결론은 이 두 수에 의존하지 않는다** — 아래 8건은 **전건 열거**되어 있고, 판정에 쓰이는 것은 오직 이 열거된 8건과 그 재실행 결과다.

allowlist ∧ 기대값 **8건 전건** (위치는 **rev `38b6c8a` 시점 좌표**다 — 인벤토리 용도이며, 후속 편집으로 밀리면 «진짜 쌍» 3건은 아래 인용문 식별자로 재도달한다):

| # | 위치 | 성격 | 진짜 「술어 + 기대값」 쌍인가 |
| --- | --- | --- | --- |
| 1 | [`20260808-983`](20260808-983-measurement-recording-convention.md)`:255` | `git grep -nE` + `# 2 hits / 2 files` | **예** |
| 2 | [`20260814-958`](20260814-958-prettier-live-docs-scope.md)`:142` | `git ls-files … \| awk` + `# 중첩 63 → PNG 54 / JSON 8 / md 1` | **예** |
| 3 | [`20260814-982`](20260814-982-changelog-tilde-guard.md)`:118` | `git grep` ×2 + `# 22 줄 / 45 발생` · `# 19 줄 / 33 발생` | **예** |
| 4 | `docs/guides/claudemd-governance.md:217` | 게이트 **실행 레시피** (기대값 없음 — 인접 문단의 과거 수치를 분류기가 오인) | 아니오 |
| 5 | `docs/ops/operational-friction.md:340` | 정본 술어 **사용 레시피** (판정은 가드가 함) | 아니오 |
| 6 | `.claude/agents/developer.md:58` | `<PR번호>` **placeholder** — 실행 불가 | 아니오 |
| 7 | `.claude/skills/create-pr/SKILL.md:116` | 同 (6 과 바이트 동일 의무 대상) | 아니오 |
| 8 | `.claude/agents/reviewer.md:43` | 전수 grep **예시 상수** (`35k` 5변형) | 아니오 |

**추출 정밀도 `3/8` = `37.5%`.** 저장소 전체에서 기계 대조 후보는 **3건**이다.

### 측정 3 — 진짜 쌍 3건 재실행 (본 ADR 의 결정적 측정)

각 술어를 **(a) 그 ADR 이 산문에 고정한 rev** 와 **(b) 현재 `38b6c8a`** 에서 실행했다. 세 쌍의 **재도달 식별자**는 줄 번호가 아니라 인용문이다 (각 `git grep -cF <문구> 38b6c8a -- <파일>` → **1**):

| # | 재도달 식별자 (인용문) | 절 |
| --- | --- | --- |
| 1 | `markdown 강조 대응 검출기는 최초 사례를 여전히 놓친다` | `983` §Amendment 2 §측정 (2) |
| 2 | `중첩(깊이 2+) 전체 63` | `958` §결정 세부 1 |
| 3 | `넓은 그물 (의도분 포함)` | `982` §정본 술어 |

| # | 박제값 | 산문 고정 rev | (a) 고정 rev 실행 | (b) `38b6c8a` 실행 |
| --- | --- | --- | ---: | ---: |
| 1 `983` §Amdt2 D-강조검출기 | `2 hits / 2 files` | `aeb6c07` (§측정 헤더) | **2** ✅ | **3** ✗ |
| 2 `958` §결정 세부 1 | `63` | `7ca1cd1` (헤더 §측정 rev) | **63** ✅ | **63** ✅ |
| 3 `982` §정본 술어 | `22 줄` | `5651980` (헤더 §측정 rev) | **22** ✅ | **30** ✗ |

**고정 rev 에서 `3/3` 재현. 현재 rev 에서 `1/3`.** 즉 **세 술어 모두 옳고, 이탈 2건은 전부 오탐**이다. naive 재실행 가드의 정밀도는 **`0/2`**.

이탈 2건의 원인은 각각 다르고, 둘 다 **구조적**이다.

- **1번은 자기 참조다.** `983` 자신이 §Amendment 2 를 쓰면서 세 번째 hit 을 만들었고, 그 사실을 같은 ADR 이 이미 명시한다 (_"본 Amendment 자신이 이 검출기의 발화를 `2` → `3` 으로 늘렸다"_). **문서가 스스로 설명한 이탈에 가드가 발화한다**
- **3번은 트리 시간 종속이다.** `CHANGELOG.md` 에 새 entry 가 들어오면 계수가 증가한다. 이탈은 정상 운영의 부산물이다

### 측정 3-1 — rev 결속은 자연어 대용(anaphora)이라 기계가 잇지 못한다

3번의 rev 결속 경로를 전수 추적하면 **4-hop 자연어 사슬**이다:

```text
982 헤더 :9   "측정 rev: `5651980` (origin/develop tip, 2026-08-14)"
   → :34     "격리 재현 (본 rev 워크트리, …)"
   → :84     "술어 (본 rev 워크트리, `pnpm install --frozen-lockfile` 후):"
   → :106    "세 후보 술어를 같은 rev 에서 CHANGELOG.md 에 적용한 결과"
   → :118    (펜스 — 명령 자신에는 rev 가 없다)
```

_"본 rev"_ · _"같은 rev"_ 는 선행사 해소를 요구하는 **대용 표현**이다. 이것을 기계가 잇는 것은 [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 가 _"판정 = 의미론이라 기계 불가"_ 로 마감한 것과 **같은 벽**이다.

> **더 결정적인 사실 — rev 를 술어 안에 넣으면 명령 형태 자체가 달라진다.** 위 2번을 rev 고정으로 재실행하려면 `git ls-files …` 가 아니라 **`git ls-files --with-tree=7ca1cd1 …`** 여야 하고, 1·3번은 `git grep <패턴> <rev>` 로 인자가 하나 더 붙는다. 즉 현행 관행(_"산문에 rev, 명령은 워킹트리 형태"_)은 **표기만 고쳐서 기계화되는 대상이 아니라 술어 자체를 다시 써야 하는 대상**이다. 소급 대상 3건 전부가 재작성을 요구한다.

### 측정 4 — 실행 안전성: 동기가 된 술어 자신이 allowlist 를 통과할 수 없다

#1064 스코핑 질문 3의 답이다.

2번 술어는 `git ls-files docs/… | awk -F/ 'NF>3'` 이며, `awk` 는 *"중첩(깊이 2+)"* 이라는 **술어의 정의 그 자체**를 담당한다. 파이프를 떼면 술어가 성립하지 않는다. 그런데 `awk` 는 `system()` 을 보유한 튜링 완전 언어이므로, **`awk` 를 allowlist 에 넣는 것은 임의 명령 실행을 허용하는 것과 동치**다.

- **명령 이름 allowlist 는 이 축을 막지 못한다** — 첫 토큰이 `git ls-files` 여도 파이프 뒤가 임의 코드다
- **파이프 금지 allowlist 는 동기가 된 술어를 배제한다** — 3건 중 1건이 즉시 탈락하고, 남는 2건은 이미 §측정 3 에서 오탐 2건을 낸 쪽이다
- 참고로 `docs` 의 셸 펜스에서 파이프·명령 치환·리다이렉트를 포함하는 비율은 과반이다 (1회성 분류기 관측 — 위 ⚠️ 와 같은 한정자가 붙는다)

> ⚠️ **과잉 일반화 금지 지점 (#1031 코멘트 명시).** 라운드 3 의 교훈은 *"따옴표를 다 벗겨라"* 가 **아니다**. git pathspec 의 `*` 글로브는 git 이 직접 처리하므로 따옴표가 **오히려 안전**하다 (`git ls-files 'docs/reports/*/*.md'`). 셸 확장이 필요한 것은 **brace 뿐**이다. 그리고 [`20260814-958`](20260814-958-prettier-live-docs-scope.md)`:146` 이 이미 이 구분을 적었다 — _"따옴표만 벗기는 대안은 셸 의존이라 또 다른 재현 함정을 만든다"_. 채택된 처방은 **경로 나열**이었다. allowlist 설계가 이 구분을 잃으면 처방이 결함을 재생산한다.

### 측정 5 — diff 스코프 변형 (가장 유망한 변형 — 역시 0)

전수 대신 **추가행만** 검사하는 변형은 [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 (5) 가 *"가장 유망"* 으로 평가한 형태이므로 별도 측정한다. 직전 **30 non-merge 커밋**, 경로 `docs` `.claude` `CLAUDE.md` `CHANGELOG.md`:

```bash
git log --no-merges -n 30 --format=%H 38b6c8a | while read c; do
  git show --format= --unified=0 "$c" -- docs .claude CLAUDE.md CHANGELOG.md
done | grep '^+[^+]' | wc -l                 # 추가행 3439
```

| 축 | 값 | 미해석 / 위반 |
| --- | ---: | ---: |
| 신규 고유 hex 토큰 (a-f 강제) | 46 | **0** |
| 신규 CI run id | 2 | **0** (`31604809249` `31604814344` 전건 API 해석) |
| 신규 셸 펜스 개시행 | 36 | 진짜 쌍 유입률 ≈ `0.1/커밋` (§측정 2 의 3건이 모두 최근 구간 산물) |

**diff 스코프 축 A 는 30 커밋에서 발화 `0`.** 발화하지 않는 가드는 회귀를 막지 못하며, 유일한 효과는 CI 네트워크 의존 추가다.

### 측정 6 — FAIL/WARN 방향별 계급: *"감소 = 회귀 신호"* 가 실측 반증됐다

#1064 스코핑 질문 4의 답이다. [`20260814-982`](20260814-982-changelog-tilde-guard.md) §후속 감시는 _"증가는 정상 … **감소는 `.prettierignore` 회귀 신호**"_ 라는 비대칭을 규정했고, #1064 는 이를 `20260811-1010` 3계급 선례에 태울 수 있는지 물었다.

모집단(`prettier ignored: false` 인 `*.md`)의 이력 변동을 직전 **250 non-merge 커밋**(2026-05-25 ~ 2026-08-14)에서 측정했다. 현행 `.prettierignore` live-set 정의를 과거 트리에 소급 적용한 값이다:

```bash
RE='^(CHANGELOG\.md|README\.md|packages/[^/]+/README\.md|docs/(benchmarks|phases|reports)/.*\.md|docs/retrospectives/(.*/)?[^/]*-retrospective\.md)$'
git log --no-merges -n 250 --format=%H 38b6c8a | while read c; do
  git ls-tree -r "$c" --name-only -z | tr '\0' '\n' | grep -cE "$RE"   # -z 필수 (아래 ⚠️)
done | uniq -c            # 45×76 / 48×47 / 47×62 / 46×65  (최신 → 과거)
```

> ⚠️ **`-z` 가 없으면 이 술어는 과거 트리에서 파일을 놓친다 — 초판이 그 형태였다.** `git ls-tree --name-only` 는 기본값 `core.quotePath=true` 라 **비-ASCII 경로를 `"` 로 감싸고 8진 이스케이프**한다. 위 `RE` 는 `^(…)$` 앵커라 선두 `"` 에서 매치가 죽는다. rev `6a1d056` 실측: 앵커 술어에서 선두 `"` 경로 **2건**(`docs/reports/p4c-mobile-<한글>-20260417.md` · `p7d-mobile-<한글>-가이드.md`), `-z`(또는 `git -c core.quotePath=false`)로는 **0건**. 두 정정 술어는 250 커밋 전 구간에서 **런 열이 완전히 일치**한다.
>
> **자기 반증이 있었다** — 초판 박제는 감소를 `46 → 45`(**−1**)로 적으면서 삭제 파일을 **3건 열거**해 산술이 닫히지 않았다. 정정하면 `48 → 45`(**−3**)로 열거와 **정확히 맞는다**. 초판의 `+2` 는 파일 유입이 아니라 **한글 → ASCII rename 으로 술어에 「보이게 된」 것**이었다. 발견은 PR [#1074](https://github.com/coseo12/astro-simulator/pull/1074) reviewer 라운드 2.

- **[실측]** 파일 집합 멤버십은 git 이력에서 직접 도출됐고, HEAD 값 **45** 는 `node scripts/verify-md-tilde.mjs --population` 실측(`prettier ignored: false : 45`)과 **일치**한다
- **[가정]** 과거 구간은 현행 `.prettierignore` 를 소급 적용한 **반사실 값**이다. 실제로는 #958 이전 negation 이 죽어 있어 관측값이 `5` 로 고정돼 있었다. 미래 변동률의 예측치로만 쓴다

**전이 3회 / 250 커밋 = `1.2%`.** 낮다 — [`20260808-983`](20260808-983-measurement-recording-convention.md) §기각 1 이 경계한 *"정상 변경마다 문서를 고쳐야 발화가 멎는 의례"* 는 성립하지 않는다.

**그러나 유일한 감소가 오탐이다.** `48 → 45` 전이의 원인 커밋은 `0c316f0` (**#905 감사 P0 위생 — 고아 정리**, PR #906) 이며, 삭제된 live-set 파일은 `docs/benchmarks/newton-1000y.md` · `p11-b-bench-20260424.md` · `p11-b-osculating-observation-20260424.md` **3건의 정당한 고아 문서**다 (같은 커밋의 live-set **추가 `0`건** — 그래서 `−3` 이 정확히 닫힌다). `.prettierignore` 회귀가 아니다.

> ⚠️ **위 술어의 `RE` 는 rev `38b6c8a` 시점 `.prettierignore` 게이트의 파생이다.** PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072)([#1063](https://github.com/coseo12/astro-simulator/issues/1063))가 `docs/retrospectives/` 게이트를 `**/*-retrospective.md` → `**/*.md` 로 **통일해 머지됐으므로**(develop `79ed14c`), 현행 트리에서 재실행하려면 `RE` 의 마지막 대안을 `docs/retrospectives/.*\.md` 로 바꿔야 한다 (그 결과 계수는 `45` → `49`, 편입 4건 = `README.md` · `P1-a11y.md` · `P1-browser-compat.md` · `P1-perf.md`. 실측 확인: 갱신한 `RE` 로 `origin/develop` = **`49`** = `--population` 실측과 일치). **술어를 갱신하지 않고 재실행하면 표가 재현되지 않으며**, 그것이 PR [#1069](https://github.com/coseo12/astro-simulator/pull/1069) 가 [`20260811-1010`](20260811-1010-measurement-c-verdict-tiers.md) §축 3 에서 발견한 것과 같은 형태다. 아래 결론은 **파일 삭제/추가 이력** 위에 서 있어 게이트 정의 변경에 영향받지 않는다.

> **⇒ *"감소 ⇒ 회귀"* 는 관측된 유일 사례에서 반증된다. 방향별 FAIL 계급의 정밀도는 `0/1`.**
>
> [`20260811-1010`](20260811-1010-measurement-c-verdict-tiers.md) 의 WARN 축이 성립한 이유는 `phraseHits ≥ 1 ∧ structureHits = 0` 이라는 **관측 가능한 중간 상태가 실재**했기 때문이다. 본 건의 *"감소"* 는 중간 상태가 아니라 **정상과 회귀가 같은 관측값을 공유**하는 구간이며, 계급을 가를 축이 없다.

### 측정 6-1 — `T0` 자기 적용이 이 결함을 통과시켰다 (§결정 3 잔여 위의 한 층)

위 `-z` 결함은 본 ADR 의 §술어 정본 규약 2항(_"박제 전에 술어를 추출해 그대로 실행하고 값이 재현되는지 확인했다"_)과 §자기 적용 표(`T0` 전건 불변)를 **모두 통과했다.** 통과할 수밖에 없었다:

> **`T0` 재실행이 검증하는 것은 _"같은 술어가 같은 값을 내는가"_ 이지 _"그 술어가 **옳은 모집단**을 보는가"_ 가 아니다.** 결함이 술어 **안**에 있으면 재실행은 **틀린 값을 결정적으로 재생산**한다 — 안정성과 정확성은 직교한다.

§결정 3 은 [`20260808-983`](20260808-983-measurement-recording-convention.md) (iv) 위의 잔여를 _"적은 술어가 그 값을 낸다"_ (= 오기록 **7** 술어 무효) 로 잡았다. 본 사례는 그 **위의 한 층**이다 — 술어는 실행됐고(오기록 7 아님), 값도 그 술어 아래서 정확했으며(오기록 5 아님), **술어가 의도한 모집단을 못 봤다.** 계층을 정리하면:

| 층 | 질문 | 실패 시 클래스 | 검증 수단 |
| --- | --- | --- | --- |
| 1 | 술어가 **적혀** 있는가 | 오기록 5 (술어 미명시) | `983` (iv) 본문 |
| 2 | 적힌 술어가 **실행되는가** | 오기록 7 (술어 무효) | `983` (iv) 확장 — 작성자 1회 실행 |
| 3 | 실행되는 술어가 **의도한 모집단을 보는가** | **본 사례** | ❌ 재실행으로 검출 불가 |

3층을 닫은 것은 재실행이 아니라 **산술 정합 검산**이었다 — reviewer 가 _"감소 `−1` 인데 삭제 열거는 3건"_ 이라는 **문서 내부 모순**으로 잡았고, 이는 [`20260808-983`](20260808-983-measurement-recording-convention.md) (i) 확장이 요구한 _"차분·비율은 **피연산자와 함께** 적어 재측정자가 정합을 눈으로 검산할 수 있게 한다"_ 가 **설계대로 작동한 사례**다. 초판이 삭제 3건을 열거해 두지 않았다면 이 결함은 남았다.

**그러므로 3층에 대한 처방은 새 규약이 아니라 (i) 확장의 이행이다** — 계수를 박제할 때 **그 계수를 설명하는 델타(추가/삭제 목록)를 함께 적는다.** 규약 신설 없이 닫히므로 §결정 3(표기 규약 신설 기각)은 **불변**이다.

### 측정 7 — 살아 있는 drift: 관측 도구 자신이 틀린 기대값을 출력한다

설계 시점(rev `38b6c8a`)에 발견한 **당시 현행 결함**이다.

> ✅ **[2026-08-14 — 닫혔다]** PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 머지(develop `fe922bb`)로 하드코딩이 제거됐다. 현재 트리 실측 — `git grep -cF 'ADR 박제값은 5 다' -- scripts/verify-md-tilde.mjs` → **`0` hit**. 아래 서술은 **`38b6c8a` 시점 기록으로 보존**한다 (소급 치환 금지 — [`20260808-983`](20260808-983-measurement-recording-convention.md) §결과 3). §결정 5 가 이 결함을 근거로 확정한 방향(f-B)은 그 머지로 **이미 발효**돼 있다.

```bash
node scripts/verify-md-tilde.mjs --population
#   prettier ignored: false : 45          ← 실측 (정확)
#   ADR 박제값은 5 다.                     ← 하드코딩 (틀림)
```

- ADR 정본 감시값은 **45** ([`20260814-982`](20260814-982-changelog-tilde-guard.md) §Amendment 2026-08-14)
- 스크립트는 **`5`** 를 두 곳에 하드코딩한다 — `scripts/verify-md-tilde.mjs` 의 헤더 주석과 `runPopulation()` 출력 문자열 (rev `38b6c8a` 재도달: `git grep -cF 'ADR 박제값은 5 다' 38b6c8a -- scripts/verify-md-tilde.mjs` → **1**)
- exit code 는 `0` 이고 어떤 경고도 없다

**#1064 가 *"감시값이 문서에만 있다"* 고 적었으나, 실제로는 더 나쁘다 — 감시값이 문서와 스크립트 **두 곳에** 있고 이미 갈렸으며, 관측 도구가 틀린 기대값을 옳은 실측값 **바로 옆 줄에** 출력하면서 아무것도 발화하지 않는다.** 이것은 산문 술어 실행 문제가 아니라 **SSoT 중복** 문제이며, volt [#120](https://github.com/coseo12/volt/issues/120) (_"drift 감지보다 중복 출처 제거가 근본"_)의 관할이다.

> **독립 재현 (병행 PR)** — PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 가 같은 drift 를 독립 발견하고 **누락 횟수까지 실측했다**: 하드코딩 `5` 는 `5` → `45`([#958](https://github.com/coseo12/astro-simulator/issues/958)) · `45` → `49`([#1063](https://github.com/coseo12/astro-simulator/issues/1063)) **두 갱신에서 연속 2회** 누락됐다. 이 `n=2` 가 §결정 5 의 방향 판정을 (f-A)에서 (f-B)로 뒤집은 결정적 증거다 — **사본을 옮기면 세 번째 누락이 스크립트 쪽에서 나고, 사본을 없애야 누락 자체가 불가능해진다.** ⚠️ 위 `45` 는 rev `38b6c8a` 기준이고, #1072 머지 후 develop(`79ed14c`)에서는 **`49`** 가 나온다. **명령 문자열은 같지만 술어는 같지 않다** — `--population` 이 읽는 `.prettierignore` 를 #1072 자신이 바꾸므로 **모집단 정의가 달라진다**. 즉 `45` 와 `49` 는 «트리 rev» 가 아니라 «트리 rev + 게이트 정의» 조합의 값이며, 병치 시 이 관계를 명시해야 한다 ([`20260808-983`](20260808-983-measurement-recording-convention.md) (iv) 확장, 술어 비교환). 본 ADR 의 `45` 는 **`38b6c8a` 트리 + 그 시점 게이트**로 정합하다. (`38b6c8a` 트리에 **신** 게이트를 적용하면 `49` 이므로, 두 값의 차는 트리가 아니라 **게이트**에서 온다.)

### 측정 8 — #1031 경미 사항: `--limit` 미명시 (채택 여부와 무관)

[`20260812-970`](20260812-970-pr-base-rule-guard.md) §8-1 5-b 의 GraphQL 교차 확인 술어 `gh pr list --state all` 은 `--limit` 을 명시하지 않아 기본값 `30` 으로 동작한다.

```bash
gh pr list --state all --limit 2000 --json number --jq 'length'                       # T4 — 아래 ⚠️
BOT='^chore/(r1-baseline-linux|baseline-remeasure)-[0-9]+$'
gh pr list --state all            --json headRefName \
  --jq "[.[] | select(.headRefName | test(\"$BOT\"))] | length"                       #  0
gh pr list --state all --limit 1000 --json headRefName \
  --jq "[.[] | select(.headRefName | test(\"$BOT\"))] | length"                       # 30
```

기본 limit 로는 봇 패턴이 **`0` 건** 잡혀 *"두 경로가 셀 단위로 일치"* 가 재현되지 않는다. `--limit 1000` 을 명시하면 **`30`** 으로 ADR 박제값과 일치한다. 본 PR 에서 정정한다.

> ⚠️ **총계는 `T4` 라 절대값을 박제하지 않는다.** `gh pr list --state all --limit 2000 … | length` 는 조회 시점마다 증가한다 — 본 판정 사이클 안에서만 `636`(2026-08-14 초판) → `637`(reviewer 라운드 2) → `638`(정정 시점)로 세 번 움직였다. 논증이 필요로 하는 것은 절대값이 아니라 **관계** 하나다: *"총계가 기본 limit `30` 을 크게 초과한다."* 그 관계는 `T4` 변동과 무관하게 참이며, 결론을 지탱하는 두 값(`0` / `30`)은 위 명령으로 재현된다. §술어 정본 규약 3항의 적용 사례다.

> ⚠️ **#1031 본문은 이를 _"봇 패턴 22건만 잡혀"_ 로 적었는데 실측은 `0` 이다.** 재현 불가 술어를 다루는 이슈가 **재현 불가 수치를 인용**했다 — 본 ADR 이 다루는 클래스의 세 번째 자기 사례이며 (앞선 둘: #983 초판의 자기 위반 / #1006 본문의 라벨↔술어 불일치), *"발화 지점 없는 규범은 주의력에만 의존한다"* 의 증거를 하나 더 보탠다.

---

## 시간 종속 5분류 — 박제 술어가 재현되지 않는 축

두 축의 기각 근거를 하나의 격자로 정리한다. **`T0` 만이 기계 대조 가능하다.**

| 계급 | 정의 | 실례 | 기계 대조 |
| --- | --- | --- | --- |
| **`T0` 정적** | rev 고정 ∧ 대상이 트리 안. 언제 실행해도 같은 값 | `git grep -nE … aeb6c07` | **가능** — 단 **안정성이지 정확성이 아니다** (§측정 6-1) |
| **`T1` 트리 시간 종속** | rev 미고정. 트리 변경이 값을 바꾼다 | `982` §정본 술어 (`22` → `30`) | 불가 — 이탈이 정상 |
| **`T2` 자기 참조** | 계수 모집단에 문서 자신이 포함. **박제 행위가 값을 바꾼다** | `983` §Amdt2 D-강조검출기 (`2` → `3`) / [`20260808-983`](20260808-983-measurement-recording-convention.md) §결과 4 근거의 인덱스 등재 계수 | 불가 — 박제 시점 참, 지금 거짓 |
| **`T3` 이동 창** | 술어에 *"직전 N건"* 이 들어가 모집단이 흐른다 | `--check-corpus` 60 PR / §기각 2 의 _"직전 10 머지 PR"_ | 불가 — 재실행이 다른 모집단을 본다 |
| **`T4` 트리 밖** | GitHub API · PR 코멘트 · 로그 등 외부 상태 | `gh api` / [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 재검토 조건 2 (스스로 caveat 부착) | 불가 — 네트워크·권한 의존 |

- `T2` 는 **박제 자체가 오염원**이라 표기 규약으로도 제거되지 않는다. 유일한 처방은 [`20260808-983`](20260808-983-measurement-recording-convention.md) §결과 4 근거가 이미 쓴 것 — **고정값 대신 rev 동반 재실행 명령을 싣는다**
- PR [#1069](https://github.com/coseo12/astro-simulator/pull/1069) 가 [`20260811-1010`](20260811-1010-measurement-c-verdict-tiers.md) §축 3 의 recipe 로 표가 재현되지 않음을 발견했는데, 그 원인 중 하나가 `T2`, 다른 하나가 `T3`(*"최근 머지 PR 60건"*) 이다. **한 문서 안에서 두 계급이 겹칠 수 있다**

---

## 후보 비교

| # | 후보 | 정밀도 실측 | 판정 |
| --- | --- | --- | --- |
| (a) | **전수 인용 식별자 대조** (#1031 (a)(b)) | 발화 17 / 진짜 위반 **0** | **기각** — allowlist 17항 필요 |
| (b) | **diff 스코프 인용 식별자 대조** | 30 커밋 발화 **0** | **기각** — 발화 0 |
| (c) | **박제 술어 재실행 대조** (#1064) | 발화 2 / 진짜 위반 **0** (`0/2`) | **기각** — rev 결속 불가 + 실행 안전성 |
| (d) | **표기 규약 신설 후 (c) 재시도** | 소급 대상 3건 / 유입 `0.1`커밋 | **기각** — 3건 재작성 비용 > 이득 |
| (e) | **`verify-adr-index` 확장** (행 자기모순 / 실행 불가 술어) | — | **기각** — 아래 §결정 4 |
| (f-A) | **감시값 SSoT = 가드 스크립트 상수** (ADR 이 상수를 참조) | 현행 drift **1건 실재** | **기각 (원안 번복)** — §결정 5 |
| (f-B) | **감시값 SSoT = ADR · 스크립트는 숫자 무복제** | 同 (같은 drift 가 근거) | **채택** — §결정 5 |
| (g) | **«현행값 = `--population` 실행값» 재구조화** (감시값 폐지) | 대조가 항등식 | **기각** — §결정 6-1 |

**(a) 를 살리려면 allowlist 17항이 필요하다.** 그리고 그중 12항은 `Superseded` + _"불변 유지"_ 가 선언된 [`20260515`](20260515-harness-managed-divergent-pattern.md) 안에 있어 **고칠 수 없다**. 예외 목록으로만 유지되는 가드는 CLAUDE.md §가드 설계 원칙 (_"drift 가드는 fail-fast 만 — fallback 분기 절대 금지"_) 및 [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 재검토 조건 5 (_"잡음이 크면 그것은 가드를 **끄는** 근거이지 예외를 추가할 근거가 아니다"_) 와 정면 충돌한다.

### 선례 대조

| 판정 | 정밀도 | 결과 |
| --- | --- | --- |
| [`20260808-983`](20260808-983-measurement-recording-convention.md) §기각 2 (PR 본문·CHANGELOG 수치 대조) | `14%` (3/21) | 기각 |
| [#1020](https://github.com/coseo12/astro-simulator/issues/1020) ADR 자기 선언 마커 | `26.3%` | 기각 |
| [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 (병치 검출) | 엄격 `0%` / 최대 관대 `21.1%` | 기각 |
| **본건 (a) 인용 식별자 전수** | **`0/17`** | **기각** |
| **본건 (c) 술어 재실행** | **`0/2`** | **기각** |

---

## 결정

### 1. 축 A — 인용 식별자 API 대조 가드를 도입하지 않는다 (#1031 기각)

전수·diff 스코프 양 변형 모두 기각한다. 근거 3항:

1. **미교정 잔존 오류가 0 이다** (§측정 1). 알려진 발생 2건(#1023 (a) 유령 run / (b) 귀속 오류)은 **둘 다 reviewer 가 머지 전에 차단**했다. *"오류를 성실히 기록하는 저장소일수록 오류 형태 검출기는 「고쳐진 오류의 기록」을 우선 발견한다"* ([`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2, volt [#995](https://github.com/coseo12/volt/issues/995))
2. **발의 사례 (b) 가 존재 검사의 사각이다** — `201ca1c` 는 정상 해석되며 틀린 것은 서술이다. 존재 검사는 이 클래스를 **원리적으로** 못 잡는다
3. **allowlist 17항 요구가 fail-fast 원칙과 충돌한다** (§후보 비교)

**#1031 스코핑 질문 2·3 은 따라서 무효화된다** — 검출 지점(전수 vs diff)과 네트워크 의존 배치는 채택을 전제한 질문이고, 양 변형 모두 발화 정밀도가 `0` 이라 배치 논의에 도달하지 않는다. **질문 4(FAIL vs warn)도 같은 이유로 무효**다.

### 2. 축 B — 박제 술어 재실행 대조 가드를 도입하지 않는다 (#1064 기각)

근거 4항:

1. **rev 결속이 자연어 대용이다** (§측정 3-1). 4-hop anaphora 사슬의 해소는 [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 가 마감한 *"판정 = 의미론"* 벽과 같다
2. **naive 재실행 정밀도 `0/2`** (§측정 3). 발화 2건이 전부 옳은 문서다
3. **실행 안전성이 원리적으로 확보되지 않는다** (§측정 4). 동기가 된 술어 자신이 `awk` 를 본질적으로 요구하고, `awk` 허용은 임의 코드 실행이다
4. **기대값이 스칼라가 아니라 자연어 튜플이다** — `# 중첩 63 → PNG 54 / JSON 8 / md 1` 은 값 4개에 계수법 3종이고, `# 22 줄 / 45 발생` 은 *"줄"* 과 *"발생"* 이 서로 다른 계수법이다. 어느 수가 어느 명령의 어느 계수법에 대응하는지는 **자연어 결속**이다

### 3. 표기 규약을 신설하지 않는다 (#1064 질문 2 / #1031 질문 5)

PR #1061 라운드 3 의 형식(코드 블록 + 인접 기대값 주석)은 **사람이 읽는 관행으로 유지**하되 규약으로 승격하지 않는다.

- **소급 대상이 3건**이고, 그 3건 전부가 §측정 3-1 대로 **술어 재작성**(`--with-tree=` / `<rev>` 인자 추가)을 요구한다. 규약 신설 비용이 대상 수를 초과한다
- **유입률이 `0.1`/커밋**이다 (§측정 5)
- 결정적으로, **라운드 3 자신이 표기 규약의 한계를 실증했다** — 라운드 2 의 처방이 *"술어를 명시하라"* 였고 라운드 3 은 그 처방을 지킨 상태에서 발생했다. 남는 잔여(*"적은 술어가 그 값을 낸다"*)를 닫는 것은 **작성자의 1회 실행**이지 표기 형식이 아니다

> **대신 관행으로 남기는 것** — 수치를 박제할 때 **그 술어를 추출해 1회 실행한다.** 본 ADR 이 §술어 정본 규약 으로 자기 적용했고, 이미 [`.claude/agents/reviewer.md`](../../.claude/agents/reviewer.md) §절차 4 와 [`20260808-983`](20260808-983-measurement-recording-convention.md) §(iv) 적용 범위가 reviewer 에게 같은 의무를 지운다. **새 조문을 세우지 않는다** — 조문이 아니라 발화 지점이 없는 것이 문제이고, 발화 지점은 위 1·2 로 기각됐다

### 4. `verify-adr-index.mjs` 를 확장하지 않는다 — 세 번째 범위 경계를 명시한다 (#1064 질문 5 / #1031 질문 6)

qa·reviewer 가 지적한 두 미검출은 각각 **이미 기각된 클래스와 동형**이다.

- **(a) 인덱스 행 본문의 자기모순** (`편입 39` ↔ `편입 40` 한 행 공존) — 두 수가 각각 자기 술어 아래 정확하고 틀린 것은 **둘 사이의 관계**다. [`20260808-983`](20260808-983-measurement-recording-convention.md) 오기록 6(술어 비교환)과 정확히 같은 형태이며, §Amendment 2 가 **정밀도 `0/76`** 으로 기각했다. 재측정 없이 그 결론을 승계한다
- **(b) 실행 불가 술어** — 위 §결정 2 와 동일

가드 헤더는 이미 두 경계(**미등재** / **산문 역참조**)를 §범위 경계로 고정하고 있다. 본 결정이 **세 번째 경계 — 「행 본문의 수치·술어 내용」** 을 추가한다. 미래 관찰자가 이를 *"누락"* 으로 오인하지 않도록 self-test 경계 픽스처로 고정한다 (구현 범위 — 아래 §Developer 인계).

### 5. **채택** — 감시값 SSoT 를 **ADR 쪽으로** 수렴시킨다 (스크립트는 숫자를 복제하지 않는다)

§측정 7 이 발견한 **현행 drift** 를 닫는다. 이것은 *"산문 술어 실행"* 이 아니라 **SSoT 중복 제거**(volt [#120](https://github.com/coseo12/volt/issues/120))이므로 위 기각과 모순되지 않는다.

> ⚠️ **원안 번복 (2026-08-14, 본 PR 내 — 머지 전이라 소급 편집이 아니다).** 초판은 방향을 **(f-A) 가드 스크립트 상수 SSoT** 로 잡았다 (`ADR_POPULATION_WATCH = 45` 신설 + ADR 이 그 상수를 참조). 병행 PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 가 **반대 방향 (f-B) 를 이미 구현**한 것을 확인하고 **(f-B) 로 번복한다.** 원안이 깨진 논거 3항:
>
> 1. **선례가 전이되지 않는다.** (f-A) 의 근거로 든 [`20260811-1010`](20260811-1010-measurement-c-verdict-tiers.md) §결정 7 은 **판정식(알고리즘)** 을 스크립트로 모은 것이고, 그 근거는 _"스크립트가 **판정을 수행**한다"_ 였다. 그런데 본 건의 `--population` 은 **판정하지 않는다** (§결정 5-3 대로 항상 exit `0`, 판단은 사람). 판정 주체가 아닌 도구에 규범값을 두는 것은 선례의 **오적용**이다
> 2. **규범 갱신이 코드 PR 을 강제한다.** 감시값은 ADR §재검토 조건 1 의 **재판정 산출물**이라 Amendment 마다 바뀐다 — 실제로 이틀 사이 두 번 움직였다 (`5` → `45` #958 / `45` → `49` [#1063](https://github.com/coseo12/astro-simulator/issues/1063)). (f-A) 는 그때마다 스크립트 편집 + 가드 PR 을 요구해 **문서 결정과 코드 변경을 영구 결합**시킨다
> 3. **실측이 (f-A) 의 실패를 이미 보여줬다.** 하드코딩 `5` 는 위 두 갱신에서 **연속 2회 누락**됐다 (#1072 실측). 사본을 *"어느 쪽에 둘지"* 를 바꾸는 것으로는 이 누락이 사라지지 않는다 — **사본을 없애야** 사라진다
>
> 즉 (f-A)·(f-B) 는 둘 다 *"중복 제거"* 를 표방하지만, (f-A) 는 **사본의 거처를 옮길 뿐**이고 (f-B) 만이 **사본을 0 으로 만든다**. §측정 7 의 실측은 방향이 아니라 **중복의 존재**를 겨눈 것이므로 (f-B) 를 그대로 지지한다.

1. **감시값 정본 = ADR [`20260814-982`](20260814-982-changelog-tilde-guard.md) §재검토 조건 1 (+ 그 §Amendment).** 결정의 산출물이므로 결정 문서가 소유한다
2. **`scripts/verify-md-tilde.mjs` 는 숫자를 복제하지 않는다.** `--population` 은 **실측 계수와 「ADR 을 열어 대조하라」는 지시만** 출력한다. 현재 `:62` 주석 / `:390` 출력 문자열의 하드코딩 `5` 는 **값 갱신이 아니라 제거** 대상이다
3. **차단하지 않는다.** `--population` 은 exit `0` 을 유지한다. 근거는 §측정 6 — *"감소 = 회귀"* 비대칭이 정밀도 `0/1` 로 반증됐으므로 방향별 FAIL/WARN 계급을 세울 축이 없다
4. **관측은 기계, 판단은 사람** ([`20260814-982`](20260814-982-changelog-tilde-guard.md) §갱신되지 않는 것) 은 불변이다. 본 결정이 바꾸는 것은 *"관측 도구가 **틀린 기대값을 함께 출력하지 않는다**"* 뿐이다
5. **착지 상태 — 스크립트 쪽은 PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 가 이미 이행했다.** 본 ADR 은 그 이행의 **결정 근거를 사후 박제**하는 것이 아니라, 독립 측정(§측정 7)으로 **같은 결론에 도달했고 방향을 그쪽으로 맞춘 것**이다. 잔여는 ADR 쪽 문구 정합 하나이며 아래 §동시 진행 PR 과의 관계 에 인계한다

### 6. *"대조 주체는 사람"* 을 명시적 결정으로 승격한다 (#1064 완료 기준 1항 — 기각 판본)

[`20260814-982`](20260814-982-changelog-tilde-guard.md) §재검토 조건 1 은 관측 술어를 박제하면서 **대조 주체를 적지 않았다**. 본 ADR 이 그 공백을 결정으로 채운다.

> **박제된 감시값과 실측의 대조 주체는 사람이다.** 대조 시점은 (i) 해당 ADR 의 재검토 트리거가 발동했을 때 (ii) 그 감시값을 인용·수정하는 PR 의 reviewer 단계 두 곳이며, 기계는 **관측값 출력까지만** 담당한다 (§결정 5-3·5-4).

동시에, 이 승격이 감추지 않도록 **다음 사실을 함께 박제한다**:

> ⚠️ [`20260808-983`](20260808-983-measurement-recording-convention.md) §(ii)·(iv) 는 **발화 지점이 0 이며 주의력에만 의존한다.** PR #1061 은 그 상태에서 **4라운드 연속** 수치 오류를 냈고, 4건 모두 **손으로 센 계수**였다(기계 출력 계수 오류 `0/4`). 본 ADR 은 그 발화 지점을 만들 수 없다고 판정했을 뿐, **공백이 없다고 주장하지 않는다.** 이 문장이 지워지면 다음 관찰자는 *"검토했더니 문제 없었다"* 로 오독한다

### 6-1. 기각한 대안 — «현행값은 항상 `--population` 실행값» 재구조화

§결정 5·6 의 **정반대 극**이라 같은 결정 공간에 속한다. 발의는 PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) dev 가 남긴 판단 여지(_"[`20260814-982`](20260814-982-changelog-tilde-guard.md) 가 Amendment 를 계속 쌓느니 §재검토 조건 자체를 «현행값은 항상 실행값» 형태로 재구조화하는 안"_)이고, reviewer 가 _"#1064 와 같은 뿌리 — 별도 이슈 금지"_ 로 본 ADR 에 회부했다. 이 대안은 #1064 스코핑 질문 1(대조 대상 범위)의 **단일 대상 채택안**이자 질문 4(방향별 계급)의 부속 판정이며, §결정 6(_"대조 주체는 사람"_)이 그 반대극이다 — **양극이 모두 본 ADR 안에 있으므로 여기서 닫는다.**

**기각한다.** 근거 3항이며 1항은 논증, 2·3항은 실측이다.

1. **대조가 항등식이 되어 재검토 트리거 자체가 소멸한다.** [`20260814-982`](20260814-982-changelog-tilde-guard.md) 의 트리거 문언은 둘 다 **2항 관계**다 — §재검토 조건 1 _"`ignored: false` markdown 이 **5개를 넘으면**"_ / §후속 감시 _"계수가 **`45` 에서 이탈**할 때"_. 기준항을 *"실행값"* 으로 정의하면 기준항 ≡ 현재항이므로 **이탈은 항상 거짓**이다. 트리거를 없애는 것이 목적이라면 정직한 방법은 §재검토 조건 1 을 **폐기**한다고 적는 것이지, 정의를 항등식으로 바꿔 **트리거가 살아 있는 것처럼 보이게 두는 것**이 아니다

2. **«이탈» 기준을 다른 데 보존하는 두 경로는 각각 독립된 이유로 닫힌다.** 항등식은 기준항을 트리 어딘가에 남기면 피할 수 있고, 이 저장소에 **두 선례가 실재**하므로 가짜 후보가 아니다:
   - **(C1) baseline 파일** — `scripts/verify-fps-baseline.mjs` 의 `const baselinePath = join(…)` 가 `docs/benchmarks/fps-lowend-baseline.json` 을 기준으로 삼고 `bench-set-baseline.mjs` 가 갱신하는 패턴. 항등식은 피하지만 **박제값이 사라지지 않고 거처만 바뀐다** — §결정 5 의 (f-A) 기각 논거가 **그대로 재적용**된다(사본의 이동 ≠ 사본의 제거). 더 나쁜 점은 baseline JSON 이 *"왜 그 값인가"* 를 담지 못해 **값 ↔ precision 논거**라는 새 drift 쌍이 생긴다는 것이다
   - **(C2) diff 스코프 방향 판정** — 기준을 base ref 에서 **실행 시점에 계산**하므로 박제값이 완전히 사라진다 (`verify-md-tilde.mjs` 의 `--base <sha>` 모드 분기 / `ci-diff-scope.mjs` 가 이미 쓰는 형태). 그러나 **§측정 6 이 이미 기각했다** — 250 커밋의 유일한 감소가 정당한 고아 정리였다(정밀도 `0/1`). 게다가 이 규약이 겨눈 **원 사건**([#907](https://github.com/coseo12/astro-simulator/issues/907) — 선언은 재포함인데 동작이 `ignored: true`)은 *"감소"* 가 아니라 **«증가가 일어나지 않음»** 이라 방향 판정으로는 **원리적으로 미검출**이다. 즉 (C2) 는 정밀도만 낮은 게 아니라 **재현율이 원 사건에 대해 `0`** 이다

3. **실측 반례 — 트리거의 산출물은 «숫자 갱신» 이 아니라 «precision 논거의 재검증» 이다.** 발의의 전제는 *"Amendment 가 쌓이는 것이 비용"* 인데, 쌓인 두 Amendment 가 실제로 무엇을 낳았는지 보면 전제가 깨진다:

   | 발동 | 계수 | Amendment 가 실제로 산출한 것 |
   | --- | --- | --- |
   | [#958](https://github.com/coseo12/astro-simulator/issues/958) | `5` → `45` | 편입 40 파일 **`~~` 0 hit** 재측정 + `docs/**` 의도분 48 발생과 **disjoint** 확인 |
   | [#1063](https://github.com/coseo12/astro-simulator/issues/1063) | `45` → `49` | 편입 4 파일 **`~~` 0 hit ∧ 단일 `~` 0 hit** + churn 실측(**1 파일 / 1 행**) |

   두 번 다 산출물은 **_"소유 모집단 안의 의도된 취소선 = 0"_ 이라는 결정 전체를 지탱하는 명제의 재검증**이었다. 대안 (C) 였다면 실행값이 조용히 `45` → `49` 가 되고 **그 재검증은 트리거되지 않는다.** 즉 (C) 가 없애는 것은 Amendment 라는 **형식**이 아니라 그 안에 든 **측정**이다. Amendment 축적은 비용이 아니라 **`20260814-982` §정밀도의 출처 가 아직 참임을 매번 확인한 기록**이다

> **판정 요지 한 줄** — «현행값 = 실행값» 은 *"박제값이 낡는다"* 는 증상을 없애지만, 그 박제값이 **낡을 때만 발동하는 재검증**을 함께 없앤다. §결정 5(f-B)가 이미 **사본 0** 을 달성하므로 증상은 해소돼 있고, (C) 는 그 위에 **트리거 소멸**만 얹는다.

---

## 결과 · 재검토 조건

### 결과

- **코드 수정 0** (본 ADR 자체). §결정 5 의 스크립트 쪽 이행은 PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 가 **이미 수행해 머지**했고(develop `79ed14c`), ADR 쪽 문구 정합은 아래 §동시 진행 PR 과의 관계 «인계 1» 로 인계
- **규범 조문 신설 0.** [`20260808-983`](20260808-983-measurement-recording-convention.md) §결정 의 _"4항이 정본"_ **무접촉**
- **동반 갱신 3곳** — `983` §Amendment 3 (오기록 7·8 분류) / `970` §8-1 5-b `--limit` 정정 / `README.md` 인덱스 등재. **`982` 는 본 PR 에서 무접촉** (사유는 §동시 진행 PR 과의 관계)
- **정본 방어선 불변** — 인용 식별자·박제 술어의 검증은 **작성자 1회 실행 + reviewer 재실행**이다. 본 ADR 은 이를 기계로 **대체하지 않는다는 것**을 확정할 뿐이다

### 동시 진행 PR 과의 관계 (2026-08-14, **머지 상태 반영판**)

본 ADR 은 병행 PR 4건과 파일을 공유했다. **판정과 인계를 여기 고정**한다 — 이 절이 없으면 각 PR 이 머지되는 순서에 따라 본 ADR 의 서술이 조용히 낡는다. **`#1072` · `#1069` 는 머지됐고**(develop `79ed14c`), 본 브랜치는 그 develop 을 **병합한 상태**다.

| PR | 상태 | 공유 파일 | 관계 | 결과 |
| --- | --- | --- | --- | --- |
| [#1072](https://github.com/coseo12/astro-simulator/pull/1072) | **MERGED** | [`20260814-982`](20260814-982-changelog-tilde-guard.md) | **동일 훅 앵커** (`@@ -336` · `@@ -533` 양쪽 바이트 동일) | **본 PR 무접촉으로 회피 — 앵커 충돌 0 으로 머지됨.** 아래 «인계 1» |
| [#1072](https://github.com/coseo12/astro-simulator/pull/1072) | **MERGED** | `scripts/verify-md-tilde.mjs` | §결정 5 를 **이미 이행** (하드코딩 `5` 제거) | 본 ADR 이 방향을 (f-B) 로 맞춤 (§결정 5 번복 박스). **develop 에서 이행 완료** |
| [#1072](https://github.com/coseo12/astro-simulator/pull/1072) | **MERGED** | [`20260812-970`](20260812-970-pr-base-rule-guard.md) | §후속 표 항목 2·3 (`@@ -402`) ↔ 본 PR §8-1 5-b (`@@ -371`) | **예측대로 훅 분리 — develop 병합 시 자동 머지, 충돌 `0`** |
| [#1070](https://github.com/coseo12/astro-simulator/pull/1070) | **MERGED** | [`20260808-983`](20260808-983-measurement-recording-convention.md) | (ii) `> 확장` **동일 삽입점** | **«인계 2» 판정대로 본 PR 이 「나중 PR」 이 되어 두 블록 보존으로 해소.** 실측: (ii) 절에 `확장 (#1051)` **1** + `확장 (#1064)` **1** 공존 |
| [#1069](https://github.com/coseo12/astro-simulator/pull/1069) | **MERGED** | — | `971` · `1010` · `1014` 3파일 | **본 PR 전건 무접촉** — develop 병합 시 충돌 `0` |

**인계 1 — `20260814-982` 착지는 후속이며, 순서 제약은 이미 해제됐다.** §결정 6(_"대조 주체는 사람"_)과 §측정 6(_"감소 = 회귀"_ 반증)을 그 ADR 본문에 착지시키는 편집은 **#1072 의 §Amendment 2 와 바이트 동일 앵커**를 갖는다. 초판은 두 편집을 담았으나 **되돌렸다.** ⚠️ **이 인계는 메모가 아니라 추적 대상이다** — 선언만 하고 착지가 갈리는 형태를 이 저장소가 [#1014](https://github.com/coseo12/astro-simulator/issues/1014) → [#1035](https://github.com/coseo12/astro-simulator/issues/1035) 로 이미 겪었으므로, 착지는 **[#1075](https://github.com/coseo12/astro-simulator/issues/1075) 의 완료 기준**으로 승격했다. 착지 시점의 감시값은 #1072 가 확정한 값(**`49`** — #1063 게이트 통일 반영. develop 병합 후 `node scripts/verify-md-tilde.mjs --population` 실측 **`49`** 로 ADR `982` §Amendment 2 와 일치 확인)이며, 본 ADR 의 `45` 는 **rev `38b6c8a` 시점 + 그 시점 게이트** 기록이라 소급 치환하지 않는다.

> ✅ **순서 제약 해제 (2026-08-14)** — #1072 가 머지됐으므로 «인계 1» 의 유일한 차단 사유(앵커 점유)가 **소멸**했다. 본 PR 에서 착지시키지 **않는 이유는 충돌이 아니라 범위**다 — 본 PR 은 판정 ADR 이고 리뷰가 진행 중이며, 착지는 [#1075](https://github.com/coseo12/astro-simulator/issues/1075) 에 **완료 기준 + 순서 제약**으로 이미 승격돼 추적된다. #1075 의 순서 제약은 **충족 상태**이므로 즉시 착수 가능하다.

**인계 2 — `983` (ii) 확장 2건은 공존이 정답이다. ✅ 이행 완료.** #1070 은 (ii) 에 `> 확장 (#1051)`(줄 번호 참조 금지)을, 본 PR 은 `> 확장 (#1064)`(자기 참조 노화)을 **같은 삽입점**에 넣는다. 두 클래스는 직교하고(전자는 **참조 좌표계**, 후자는 **계수 모집단**), #1070 은 오기록 분류표에 **행을 추가하지 않는다**고 명시하므로 본 PR 의 _"현재 분류표는 **8행**"_ 서술과도 충돌하지 않는다. **머지 순서 무관 — 나중 PR 이 두 블록을 모두 보존하도록 해소한다.** ✅ **#1070 이 먼저 머지돼 본 PR 이 「나중 PR」 이 됐고, develop 병합에서 두 블록을 보존해 해소했다** (무손실 술어: `grep -c '^> \*\*확장 (\[#1051\]'` → `1` / `'^> \*\*확장 (\[#1064\]'` → `2` = (ii) 오기록 8 + (iv) 오기록 7). 예측대로 **의미론 충돌 `0`** 이었다.

### 재검토 조건

트리거 증식을 막기 위해 [`20260808-983`](20260808-983-measurement-recording-convention.md) §기각 2 및 §Amendment 2 와 **정렬**한다.

1. **base rate 트리거 (축 A)** — 인용 식별자 오류가 **머지된 산출물에 잔존한 채** 발견되면 (즉 reviewer 가 놓치면) 1건이라도 재측정한다. 현재 잔존 `0` 이므로 첫 반례의 정보량이 크다
2. **base rate 트리거 (축 B)** — 박제 술어가 **실행되지 않는다는 사실이 머지 후에 발견되면** 1건이라도 재측정한다. PR #1061 라운드 3 은 머지 **전** 적발이라 이 트리거를 발동시키지 않는다
3. **구조화 박제 트리거 (기존 §기각 2 공유)** — 수치가 표/JSON 등 **구조화 블록으로만** 박제되는 관행이 정착하면 재검토한다. 관측 술어는 §기각 2 의 것을 **그대로 공유**한다 (직전 10 머지 PR 의 CHANGELOG entry 중 산문 raw 수치 포함 비율 `≤ 20%`). ⚠️ 이 술어 자신이 `T3`(이동 창) 이므로 **사람이 측정한다**
4. **rev 결속의 기계화 트리거 (신규, 축 B 고유)** — 박제 술어를 **rev 인자를 포함한 형태**(`git grep <pat> <rev>` / `git ls-files --with-tree=<rev>`)로 적는 관행이 정착해 §측정 3-1 의 anaphora 사슬이 소멸하면 (c) 를 재측정한다. 관측 술어: **신규 박제 술어 중 rev 인자를 포함한 비율 ≥ 50%** — 현재 `0/3`
5. **트리거가 아닌 것 (명시)** — _"정규식·추출기를 더 정밀하게 만들었다"_ 는 재검토 트리거가 **아니다** ([`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 재검토 조건 4 · [`20260814-982`](20260814-982-changelog-tilde-guard.md) §재검토 조건 5 정렬). 본건의 벽은 추출 정밀도가 아니라 **rev 결속의 자연어성 · 실행 안전성 · 잔존 오류 `0`** 이다
6. **채택으로 뒤집힐 경우의 구현 제약 (조건부 박제)** — CLAUDE.md §가드 설계 원칙 _"drift 가드는 fail-fast 만"_ 에 따라 **allowlist / 예외 경로 / `|| true` / soft-exit 분기를 두지 않는다.** §측정 1 의 17항은 예외 목록의 근거가 아니라 **가드를 만들지 않는 근거**다
7. **§결정 6-1 재검토 트리거 (대안 (C) 고유)** — «현행값 = 실행값» 재구조화는 **_"Amendment 가 N 개 쌓였다"_ 로는 재검토되지 않는다** (그것이 §결정 6-1 3항이 기각한 전제 자체다). 트리거는 **산출물 쪽**에 건다: [`20260814-982`](20260814-982-changelog-tilde-guard.md) §재검토 조건 1 이 발동했는데 그 Amendment 가 **precision 논거의 재검증 없이 숫자만 갱신한 사례가 2회 연속** 나오면 (C) 를 재측정한다.
   - **관측 술어** (본 ADR 이 *"술어 없는 절대수"* 를 겨누므로 자기 적용한다 — [`20260808-983`](20260808-983-measurement-recording-convention.md) §(ii)·(iv)): **모집단** = `20260814-982` 의 `^## Amendment ` 절 중 §재검토 조건 1 발동을 트리거로 명시한 것. **보유 조건** = 그 절이 *"소유 모집단 안의 의도된 취소선 = 0"* 을 **재측정한 증거**(편입 파일 대상 `~~` 계수 실행 결과)를 포함하는가. 재도달 명령:
     ```bash
     grep -nE '^## Amendment |^### (갱신된 감시값|precision 논거)' docs/decisions/20260814-982-changelog-tilde-guard.md
     ```
   - **판정 주체는 사람이다** (§결정 6 과 동일 — 「재검증을 포함하는가」는 의미론 판정이라 기계 불가). 형제 트리거 3 의 *"사람이 측정한다"* 와 같은 계급이다
   - **현재값 `0/2`** — #958(편입 40 파일 `~~` `0` hit) · #1063(편입 4 파일 `~~`·단일 `~` `0` hit + churn) **둘 다 재검증을 산출**했다. 두 Amendment 제목이 **각각 _"precision 논거 유지"_** 를 달고 있는 것이 그 표지다

---

## Developer 인계

**본 ADR 자신은 구현을 포함하지 않는다.** 잔여는 [#1075](https://github.com/coseo12/astro-simulator/issues/1075) 로 분리했고 **두 항목뿐**이다 — §결정 5 의 스크립트 쪽 이행은 PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 가 이미 수행했으므로 인계에서 뺀다.

### 잔여 1 — `20260814-982` 착지 (**#1072 머지 후**)

§결정 6(_"대조 주체는 사람"_)과 §측정 6(_"감소 = 회귀"_ 반증)을 [`20260814-982`](20260814-982-changelog-tilde-guard.md) 본문에 착지시킨다. **#1072 와 바이트 동일 앵커라 본 PR 에서는 손대지 않았다** (§동시 진행 PR 과의 관계 «인계 1»).

- **착지 위치**: §재검토 조건 1 의 dated 하위 불릿 1개 + §후속 감시(#1072 머지 후에는 **§Amendment 2 의 §후속 감시**) 뒤 dated `>` 블록 1개
- **원문 보존** — 소급 치환 금지. dated 포인터만 부기한다 ([`20260808-983`](20260808-983-measurement-recording-convention.md) §결과 3)
- **감시값은 재기재하지 않는다** — #1072 머지 후 정본은 그 §Amendment 2 의 값(**`49`**)이고, 본 ADR 의 `45` 는 rev `38b6c8a` 시점 기록이다

### 잔여 2 — `verify-adr-index.mjs` self-test 경계 픽스처 1건

*"인덱스 행 본문의 수치·술어 내용은 검사 대상이 아니다"* 를 고정한다 (§결정 4 의 **세 번째 범위 경계**). 기존 `F10`(미등재 미검출 경계)과 같은 형식이며, **검사 로직은 건드리지 않는다** — 경계를 명문화하는 픽스처만 추가한다.

### 박제 대상 문구 (raw text — 백틱·특수문자 모두 raw 인용)

잔여 1 의 착지 문구다. 아래 블록을 **그대로** 옮긴다 (markdown inline backtick 이스케이프 변형 회피 — [`guard-pr-dod.md`](../lessons/guard-pr-dod.md) §3). `<현행 감시값>` 은 #1072 머지 후의 값으로 치환한다.

````text
   - ⚠️ **[2026-08-14 — 대조 주체가 결정으로 승격됐다]** 본 항은 관측 술어를 박제하면서 **대조 주체를 적지 않았고**, 그 공백이 [#1064](https://github.com/coseo12/astro-simulator/issues/1064) 의 발의 근거가 됐다. ADR [`20260814-1031-1064`](20260814-1031-1064-committed-claim-guard-rejected.md) §결정 6 이 _"**대조 주체는 사람**이고 기계는 관측값 출력까지만 담당한다"_ 를 **명시적 결정으로 승격**했다 (대조 시점: 재검토 트리거가 발동했을 때 / 감시값을 인용·수정하는 PR 의 reviewer 단계). 같은 ADR §결정 5 는 **감시값 SSoT 를 본 문서 쪽으로** 확정했다 — `scripts/verify-md-tilde.mjs` 는 숫자를 복제하지 않는다.
````

````text
> ⚠️ **[2026-08-14 — _"감소 = 회귀 신호"_ 는 실측 반증됐다]** 위 서술은 **측정 없이 세운 가정**이었다.
> ADR [`20260814-1031-1064`](20260814-1031-1064-committed-claim-guard-rejected.md) §측정 6 이 직전
> **250 non-merge 커밋**(2026-05-25 ~ 2026-08-14, rev `38b6c8a`)에서 계수 변동을 측정한 결과 전이는
> **3회**이고, **유일한 감소**(`48 → 45`, 삭제 live-set 3건 / 추가 0건)의 원인은 커밋 `0c316f0`
> ([#905](https://github.com/coseo12/astro-simulator/issues/905) 감사 P0 위생 — **고아 문서 정리**)로
> `.prettierignore` 회귀가 **아니었다**. 정상 삭제와 회귀가 같은 관측값을 공유하므로 **방향별
> FAIL/WARN 계급을 세울 축이 없다** (정밀도 `0/1`). **위 서술은 그 시점 기록이라 보존한다** —
> 감소를 보면 원인을 규명하라는 **사람 대상 지침으로는 유효**하고, 반증된 것은 그것을 **기계 차단
> 기준으로 쓸 수 있다**는 함의뿐이다.
````

### 구현 제약

1. **exit code 불변** — `--population` 은 exit `0`. 차단 계급 신설 금지 (§결정 5-3)
2. **감시값 사본 금지** — 스크립트·CI·에이전트 파일 어디에도 숫자를 두지 않는다 (§결정 5-2). 잔여 1 의 착지 문구도 값을 재기재하지 않는다
3. **fallback 분기 금지** — 계수 산출 실패는 통과로 흘리지 않고 그 자체를 오류로 보고한다 (CLAUDE.md §가드 설계 원칙)
4. **`verify-adr-index.mjs` 검사 로직 무접촉** — self-test 픽스처 추가만

### 동반 갱신 (본 ADR PR 에서 이미 수행 — 구현 PR 대상 아님)

- [`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 3 — 오기록 **7**(술어 무효) · **8**(자기 참조 노화) 행 추가 + (iv)·(ii) `> 확장` 블록
- [`20260812-970`](20260812-970-pr-base-rule-guard.md) §8-1 5-b — `--limit 1000` 명시
- [`README.md`](README.md) — 본 ADR 인덱스 등재

### 명시적 비-범위

- **인용 식별자 대조 가드 · 박제 술어 재실행 가드 — 신설 금지** (§결정 1·2)
- **표기 규약 신설 금지** (§결정 3). 기존 3건의 술어 재작성도 하지 않는다
- **`verify-adr-index.mjs` 검사 로직 확장 금지** (§결정 4). self-test 경계 픽스처 추가만 허용
- **«현행값 = 실행값» 재구조화 — 금지** (§결정 6-1). `--population` 출력이 ADR 박제값을 **대체**하도록 문구를 쓰지 않는다. 정본은 ADR 이고 스크립트는 **가리키기만** 한다
- **`20260814-982` 를 PR #1072 머지 이전에 편집하는 것 — 금지** (앵커 충돌. §동시 진행 PR 과의 관계)
- **[`20260807-971`](20260807-971-required-status-checks.md) · [`20260811-1010`](20260811-1010-measurement-c-verdict-tiers.md) · [`20260813-1014`](20260813-1014-release-pr-class-no-op.md) — 무접촉** (PR [#1069](https://github.com/coseo12/astro-simulator/pull/1069) 동시 편집 중)
- **[#1040](https://github.com/coseo12/astro-simulator/issues/1040) 존량 회수 — 별개 판정**
- **`docs/**` 의 stale 수치 소급 정정 — 하지 않는다.** §측정 3 이 보인 대로 이탈 2건은 **문서가 옳고 rev 가 다를 뿐**이다. 소급 편집은 기록 위조다 ([`20260808-983`](20260808-983-measurement-recording-convention.md) §결과 3)

---

## 교차검증 반영 사항 (agy, 2026-08-14 — `code 1074`, anchor `ADR-new-or-amendment`, outcome `applied`)

**메인 오케스트레이터가 수행**했다 — `architect` 는 직접 호출이 금지돼 있다 ([#479](https://github.com/coseo12/astro-simulator/issues/479)). exit `0` (폴백 아님). 5개 항목 **전건 `양호`**, 판정 **승인 권고**. 로그: `.claude/logs/cross-validate-code-20260814-161816.log`.

> ⚠️ **한정자** — 위 로그 경로는 gitignore 대상이라 **인용의 무치환 여부는 트리 안에서 재검증 불가**하다 ([`20260808-983`](20260808-983-measurement-recording-convention.md) §Amendment 2 가 같은 한정자를 달았다). 반면 **아래 판정의 근거는 전부 트리 안에서 재현 가능**하다 — §측정 1~8 의 술어 전문이 그것이다. **인용은 신뢰의 대상이고 근거는 검증의 대상이다.**

### 합의

- **로직 정확성 및 실측 무결성** `양호` — 기각 근거의 실측 데이터 무결성 확인
- **실행 안전성** `양호` — allowlist 불가 논거(`awk` 의 `system()` 보유)를 포함한 안전성 결여 규명을 인정
- **모집단 감소의 회귀 판정 반증** — 유일 감소가 정당한 삭제라 정밀도 `0/1` 이라는 §측정 6 결론에 동의
- **통합 ADR 판정의 정합성** — 4 근거 + 반례 조건 명시가 [`README.md`](README.md) §규약 11(_"1 ADR = 1 결정"_)과 정합
- 최종 결론 축자: _"수치 검증 자동화의 이상과 현실적 한계(정밀도 0%, 안전성 결여, Anaphora 결속 한계)를 명확한 실측 데이터로 규명하고 올바른 거버넌스를 확립한 모범적인 ADR"_

### Claude 재분석으로 기각한 외부 모델 제안

**제안 2 — _"후속 구현 PR 시 `scripts/verify-md-tilde.mjs` 의 `ADR_POPULATION_WATCH = 45` 상수화"_ → 기각.** 본 ADR 이 채택한 것은 상수화가 **아니라** «값을 스크립트에서 제거» 다 (§결정 5 (f-B), §결정 6). **상수화는 `5` 를 `45` 로 바꾸는 것과 같은 클래스의 실수**이며 — 값이 스크립트에 남는 한 다음 갱신에서 **세 번째 누락**이 난다 (앞선 둘: #958 `5`→`45` / #1063 `45`→`49`). PR [#1072](https://github.com/coseo12/astro-simulator/pull/1072) 가 이미 반대 방향으로 구현해 머지됐다. cross-validate 는 _"SSoT 수렴"_ 을 **「한 곳에 상수로 모은다」** 로 읽었으나 **본 ADR 의 수렴 지점은 ADR 자신이지 스크립트가 아니다** (volt [#51](https://github.com/coseo12/volt/issues/51) 외부 툴 주장 실측 가드).

> ⚠️ **이 오독은 ADR 문언이 유발했을 여지가 있다** — _"감시값 SSoT 수렴"_ 만 보면 상수화로 읽힌다. 그래서 §결정 5 제목을 _"**ADR 쪽으로** 수렴시킨다 (스크립트는 숫자를 복제하지 않는다)"_ 로 두고 5-2 에 _"값 갱신이 아니라 **제거** 대상"_ 을 못박았다. **외부 모델이 오독한 지점이 곧 문언의 약한 지점**이라는 신호로 받아 문면을 조인 것이며, 판정 자체는 바뀌지 않았다.

### 고유 발견 (채택, 단 범위 조정 — 후속 이슈 미생성)

**`--limit 1000` 의 장기 상한** — _"저장소 총 PR 이 `1,000` 건을 초과하면 그 GraphQL 쿼리도 초기 1,000건만 조회해 누락이 발생한다(`T3`/`T4`)"_. **타당하다.** 다만 이는 본 ADR 만의 문제가 아니라 **이 저장소의 반복 결함**이며, PR [#1069](https://github.com/coseo12/astro-simulator/pull/1069) reviewer 가 **정확히 같은 클래스**를 차단으로 잡았다(`--limit 300` 이 90일 창을 이미 절단). **개별 PR 마다 힌트를 부기하는 것 자체가 다문서 drift** 이고 그것이 본 ADR 이 겨누는 클래스이므로, §측정 8 의 `T4` 한정자로 흡수하고 **별도 이슈를 만들지 않는다** — 근본 처방(_"limit 이 창을 덮는지 자체를 술어에 포함"_)은 #1069 관할이다.

### Claude 편향 셀프 체크 ([cross-validate-protocol.md](../guides/cross-validate-protocol.md) §5)

- **처방 충돌을 먼저 확인했다** — 제안 2 를 받기 전에 **#1072 와 본 PR 의 실제 diff 를 대조**해 두 PR 이 같은 방향으로 수렴했음을 확인했다. 대조 없이 제안만 봤으면 _"상수화하라"_ 를 dev 에 그대로 넘겼을 것이고, 그것은 **#1072 가 방금 제거한 값을 되살리는** 결과가 된다
- **어긋난 것은 처방이지 판정이 아니다** — 5개 `양호` 는 유효하고 기각 결론에 이견이 없다. ⚠️ **기각 ADR 은 _"가드를 만들지 않는다"_ 는 결정이라 근거가 틀리면 실재 결함을 영구히 덮는다** — 그래서 reviewer 에게 **모집단 정의 민감도**를 중점으로 걸었고, 실제로 라운드 2 에서 §측정 6 술어의 `core.quotePath` 결함이 그 축에서 적발됐다
- **4축 판정** — 낙관적 일정(구현 산출물 `0`) / 결합 간과(#1069·#1070·#1072 3 PR 과 파일 공유를 §동시 진행 PR 절로 전수 대조) / 폐기 프레이밍(기각 3축 각각을 실측 정밀도로 지지) / 순수주의(§결정 5 를 외부 구현에 맞춰 **번복**) — **4축 통과**
